import { StyleSheet } from "react-native";

export const homeStyle = StyleSheet.create({
  headerText: {
    textAlign: "center",
    fontSize: 16,
    fontWeight: "500",
    marginTop: 15,
    color: "white",
  },
  logoContainer: {
    alignSelf: "center",
    marginTop: 20,
  },
});
